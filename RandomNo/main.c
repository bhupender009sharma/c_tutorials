#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main()
{ int i,b=0,c,d=0,p,k,dice;
  char a;
  printf("number of times you wanna play\n");
  scanf(" %d",p);
    for(k=1;k<=p;k++){
     for(i=1;i<=3;i++){
            dice=(rand()%6)+1;   // % GIVES US THE REMAINDER
             printf("%d\n",dice);
             b=dice+b;
      }

       printf("%d\n",b/3);

       printf("now enter will the next outcome be higher or lesser than the avg value.(h/l)\n");
       scanf(" %c",a);

        for(i=1;i<=19;i++){
            dice=(rand()%6)+1;   // % GIVES US THE REMAINDER
             printf("%d\n",dice);
             d=dice+d;
      }
      int j,x;
     ########################### *****88   [incomplete]   >>>>>>>>>>DSB\###################
       x=
        (lastName[0]<'M')? printf("real madrid"): printf("Barcelona") ;



    }return 0;
}
